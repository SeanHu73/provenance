/** `mapbox-gl-draw-freehand-mode` ships no types. It exports a Draw mode object
 *  that slots into MapboxDraw's `modes` map (used in place of `draw_polygon`). */
declare module 'mapbox-gl-draw-freehand-mode' {
  const FreehandMode: object;
  export default FreehandMode;
}
